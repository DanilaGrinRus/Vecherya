# K2: Контракт Worker → Site (Inventory API) v1

Дата: 17 января 2026

Назначение: Cloudflare Worker извлекает и парсит пост(ы) Telegram и отдаёт сайту готовый JSON инвентаря. Сайт не парсит текст и не содержит правил.

---

## 1. Принципы и инварианты

### 1.1 Source of truth
- Единственный источник истинных данных — Worker.
- UI может менять отображение, но не смысл и математику.

### 1.2 Active-first (финальное правило)
Если у пользователя по одной и той же карте одновременно выявлены:
- строка количества (например: `Имя - 3`), и
- активная запись (например: `Имя - до 20.01` или объектный статус вроде «Открыта ... Лимит ...»)

то Worker обязан оставить карту только в `active`, а из количественных блоков убрать.

Следствия:
- Одна и та же `cardKey` не может одновременно присутствовать в `counts` и в `active`.
- `counts` предназначен только для «неактивных/складских» карт, которые выражены числом.

---

## 2. Эндпоинты

### 2.1 GET /api/inventory
Возвращает единый JSON для автозаполнения инвентаря на сайте.

Query-параметры:
- `active_url` (optional): если передан, Worker использует этот адрес как источник постов; иначе использует дефолтный источник, заданный в Worker.
- `force` (optional, boolean): если true, Worker принудительно обновляет кэш.

HTTP-коды:
- 200: успешный ответ (даже если частично, см. `warnings`).
- 400: некорректные параметры.
- 502: источник недоступен (Telegram/прокси/парсер источника).
- 500: внутренняя ошибка Worker.

Headers:
- `Content-Type: application/json; charset=utf-8`
- `Cache-Control`: рекомендуемо `public, max-age=30` (или по вашей стратегии)
- `ETag`: по желанию (хэш данных) для условных запросов.


---

## 3. Формат ответа (JSON)

### 3.1 Корневой объект
Поля:
- `ok` (boolean): успешность.
- `version` (string): версия контракта, например `"inv.v1"`.
- `generatedAt` (string, ISO-8601): время генерации на Worker.
- `source` (object): сведения об источнике.
- `inventory` (object): нормализованный инвентарь.
- `warnings` (array of string, optional): некритичные проблемы.
- `error` (object, optional): присутствует только при `ok=false`.

### 3.2 source
- `posts` (array of string): список URL/идентификаторов постов, которые использовались.
- `messagesChecked` (number): сколько сообщений/блоков просмотрено.
- `sampleMessages` (array, optional): диагностические превью (укороченные строки) только для отладки.

### 3.3 inventory
- `users` (array of user): агрегированный список пользователей.
- `cardCatalog` (array of card, optional): справочник карт для UI (чтобы UI мог красиво подписывать).
- `instances` (array of instance, optional): объектные сущности типа «Банк/Храм/Преисподняя».

---

## 4. Объекты данных

### 4.1 user
Поля:
- `key` (string): стабильный ключ пользователя (для поиска, дедупликации и URL-хэша). Нормализация: trim, lower, единые пробелы.
- `name` (string): отображаемое имя (как в тексте).
- `aliases` (array of string, optional): если Worker поддерживает алиасы, здесь список отображаемых вариантов.

Количественные данные (неактивные карты):
- `counts` (object map string->number): `cardKey` -> целое количество. Здесь НЕТ карт, которые попали в `active`.
- `totalCount` (number): сумма всех `counts`.

Активные/объектные/условные записи:
- `active` (array of activeEntry): список активных записей. Если карта попала сюда, её не должно быть в `counts`.

Комментарий/метаданные:
- `notes` (array of string, optional): комментарии из скобок или строк условий, если вы решите их хранить отдельно.

### 4.2 activeEntry
Назначение: хранить любые записи, которые не являются чистым числом, но относятся к пользователю по конкретной карте.

Поля:
- `cardKey` (string): ключ карты.
- `cardName` (string, optional): отображаемое имя карты.
- `type` (string): один из типов: `until`, `opened`, `limited`, `rule`, `custom`.
- `text` (string): исходный текст условия (например `"до 22.01"`, `"Открыта 16.01.2026 года. Лимит 6"`).
- `untilText` (string, optional): если тип `until`, сюда кладём то, что после `до`.

### 4.3 card
Справочник для UI.

Поля:
- `cardKey` (string): ключ.
- `cardName` (string): отображаемое имя.
- `section` (string): секция верхнего уровня (например `"activations"`, `"violations"`, `"danger"`, `"super"`, `"common"`, `"tv"`).
- `emoji` (string, optional): если выделяете эмодзи как маркер.

### 4.4 instance
Для объектов, которые не являются инвентарём в штуках (преисподняя, банк, храм и подобное).

Поля:
- `kind` (string): например `"hell"`, `"bank"`, `"temple"`.
- `title` (string): название объекта.
- `ownerKey` (string, optional): владелец (если определяется).
- `openedText` (string, optional): строка открытия.
- `limit` (number, optional): лимит, если извлекается.
- `raw` (string): исходная строка/абзац объекта.

---

## 5. Нормализация cardKey

Требование: `cardKey` должен быть стабильным между обновлениями.

Рекомендованная схема:
- берём название карты без эмодзи,
- приводим к lower,
- заменяем `ё` на `е`,
- пробелы и дефисы -> `_`,
- убираем повторяющиеся `_`.

Примеры:
- `"🟦Синяя"` -> `"sinyaya"`
- `"🅾️Обнуление"` -> `"obnulenie"`
- `"🎦Карта Контента"` -> `"karta_kontenta"`

---

## 6. Ошибки

Если `ok=false`, Worker возвращает:

`error`:
- `code` (string): `NO_MESSAGES`, `SOURCE_UNAVAILABLE`, `INVENTORY_NOT_FOUND`, `BAD_REQUEST`, `INTERNAL_ERROR`.
- `message` (string): человеко-читаемое сообщение.
- `details` (object, optional): диагностические поля.

Важно: при частичных успехах (например, одна секция распарсилась, другая нет) рекомендуется оставлять `ok=true`, а проблему писать в `warnings`.

---

## 7. Минимальный пример ответа (структура)

Этот блок иллюстрирует поля. Значения примерные.

- `ok`: true
- `version`: "inv.v1"
- `generatedAt`: "2026-01-17T01:05:00.000Z"
- `source.posts`: ["https://t.me/.../7", "https://t.me/.../8"]
- `inventory.users[0].key`: "protven"
- `inventory.users[0].counts`: { "seraya": 5, "sinyaya": 0 }
- `inventory.users[0].active`: [ { "cardKey": "obnulenie", "type": "until", "text": "до 22.01", "untilText": "22.01" } ]

Примечание: в примере показана "синяя": 0 только как иллюстрация ключей. В реальном ответе `counts` не должен содержать нулевые значения.

---

## 8. Требования к данным для автозаполнения UI

Worker обязан выдавать:
- полный список пользователей в `inventory.users`;
- для каждого пользователя:
  - `counts` с целыми положительными числами;
  - `active` как список (может быть пустым);
  - `totalCount` как сумму `counts`;
- стабильные `cardKey`.

UI, в свою очередь:
- заполняет поля инвентаря по `counts`;
- отображает активные карты отдельным блоком из `active`;
- НЕ пересчитывает и НЕ переносит карты между `counts` и `active`.

---

## 9. Тесты совместимости (обязательные)

1. Если встречается строка вида `Имя - до 20.01`, то:
   - в `active` должна появиться запись с `cardKey` текущей карты;
   - эта `cardKey` отсутствует в `counts` даже если где-то было число.
2. Если встречается строка вида `Имя - 1 (4 удар)`, то:
   - в `counts` прибавляется 1;
   - текст в скобках не меняет количество и может попасть в `notes`.
3. Объектные абзацы про «Открыта ... Лимит ...» не создают пользователей.

